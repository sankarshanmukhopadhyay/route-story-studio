function distanceMetres(a,b){ const R=6371008.8,toRad=Math.PI/180; const p1=a.latitude*toRad,p2=b.latitude*toRad,dP=(b.latitude-a.latitude)*toRad,dL=(b.longitude-a.longitude)*toRad; const h=Math.sin(dP/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dL/2)**2; return 2*R*Math.asin(Math.sqrt(h)); }
export const MATERIAL_DISPLACEMENT_METRES=2000;
export function compareResolvedLocations(intentLocations,resolvedLocations){ return intentLocations.map((location,index)=>{ if(!location?.coordinates||!resolvedLocations[index]?.coordinates) return null; const metres=distanceMetres(location.coordinates,resolvedLocations[index].coordinates); return metres>MATERIAL_DISPLACEMENT_METRES?`Resolved location ${index+1} differs from the embedded coordinate by ${(metres/1000).toFixed(1)} km.`:null; }).filter(Boolean); }
